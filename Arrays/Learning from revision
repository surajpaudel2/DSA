=> 4 Sum
    -> x + y = z then it is not possible to get z without adding y to 'x'.
    -> There are always 2 possibilities, one is negative and another is positive, so there is no point of checking for other numbers.